# Tests

The project will maintain a layered test matrix covering metadata validation, Android app behavior, runtime startup, graphics/audio/input and representative Windows software.

Core 0.1 starts with repository metadata validation. Runtime and device tests are added only when the corresponding components are integrated.
